{
  "name": "@dsh/e2e-tests",
  "version": "0.2.10",
  "private": true,
  "description": "Legacy Electron compatibility smoke tests retained outside the public Tauri release path",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "pretest": "pnpm --filter @dsh/desktop bundle"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0"
  }
}
