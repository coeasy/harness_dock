[project]
name = "HarnessDock Native Preview"
version = "0.1.0"
bundle_id = "com.dsh.client.perry.preview"
description = "Perry native preview host for HarnessDock"
entry = "src/main.ts"

[build]
out_dir = "release/perry-build"

[macos]
bundle_id = "com.dsh.client.perry.preview"
category = "public.app-category.developer-tools"
minimum_os = "13.0"

[linux]
format = "deb"
category = "Development"
description = "HarnessDock Perry native preview host"
