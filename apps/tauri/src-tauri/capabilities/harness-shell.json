{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "harness-shell",
  "description": "v0.2.0 Harness Shell plugin capability for the exact managed desktop Runtime origin family. The Runtime is launched on plain HTTP 127.0.0.1 with an ephemeral port, so no localhost alias, IPv6 or HTTPS origin receives the privileged shell bridge.",
  "local": false,
  "platforms": ["linux", "macOS", "windows"],
  "remote": {
    "urls": [
      "http://127.0.0.1:*/*",
      "http://127.0.0.1:*"
    ]
  },
  "windows": ["harness"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-start-dragging",
    "harness-shell"
  ]
}
