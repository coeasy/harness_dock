{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "harness-shell",
  "description": "Minimal capability for the loopback Harness WebView shell button. It can only show the separate local Shell Settings plugin window.",
  "windows": ["harness"],
  "permissions": ["core:default", "harness-shell"]
}
