{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "local-main",
  "description": "Local bundled HarnessDock control capability. Gateway/remote mobile documents receive no local Tauri IPC permissions; the managed desktop loopback Harness window has its own separately minimized harness-shell capability.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "platform-info",
    "gateway-health",
    "gateway-pair",
    "gateway-host",
    "harness-window",
    "splash-status",
    "runtime-status",
    "runtime-start",
    "runtime-stop",
    "runtime-maintenance",
    "startup-recovery-status"
  ]
}
