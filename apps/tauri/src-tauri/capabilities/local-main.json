{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "local-main",
  "description": "On-demand bundled Recovery/Gateway control capability. The window is not created during normal startup; managed Harness Web has a separately minimized remote-origin capability.",
  "windows": ["control"],
  "permissions": [
    "core:default",
    "platform-info",
    "gateway-health",
    "gateway-pair",
    "gateway-host",
    "harness-window",
    "splash-status",
    "runtime-status",
    "runtime-start",
    "runtime-stop",
    "runtime-maintenance",
    "startup-recovery-status"
  ]
}
