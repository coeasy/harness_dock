{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "shell-settings",
  "description": "Local on-demand Plugin Diagnostics window. Read-only Runtime status stays direct; privileged update/exit operations cross Host Protocol v2 and the Capability Broker.",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "shell-settings",
    "host-protocol"
  ]
}
