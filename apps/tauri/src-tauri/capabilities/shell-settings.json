{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "shell-settings",
  "description": "Local on-demand Diagnostics surface. State/business operations use Host Protocol; only event subscription and self-close stay direct.",
  "windows": ["settings"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "shell-settings",
    "host-protocol"
  ]
}
