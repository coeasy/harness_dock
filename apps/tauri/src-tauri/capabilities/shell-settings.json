{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "shell-settings",
  "description": "Local on-demand Shell Settings plugin. It can inspect and recover the owned Runtime but never receives remote Harness permissions.",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "shell-settings"
  ]
}
