{
  "name": "harnessdock",
  "version": "0.2.0",
  "private": true,
  "description": "Tauri 2 client for the official DeepSeek Harness Web UI: local desktop runtime plus paired Android/iOS remote runtime.",
  "type": "module",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "check:versions": "node scripts/check-versions.mjs",
    "check:release": "node scripts/check-release.mjs",
    "check:tauri": "node scripts/check-tauri-host.mjs",
    "check:size": "node scripts/check-size.mjs",
    "perf:report": "node scripts/perf-report.mjs",
    "mark:released": "node scripts/mark-released.mjs",
    "e2e": "pnpm --filter ./tests/e2e test",
    "build": "pnpm -r --if-present run build",
    "setup": "node scripts/bootstrap.mjs",
    "sync:dsh": "pnpm --filter @dsh/docs-sync start",
    "prepare:runtime": "pnpm --filter @dsh/client-runtime bundle-runtime",
    "build:tauri": "cd apps/tauri && npx --yes @tauri-apps/cli@2.11.0 build",
    "build:tauri:android": "cd apps/tauri && npx --yes @tauri-apps/cli@2.11.0 android build --debug --apk --target aarch64",
    "build:tauri:ios-sim": "cd apps/tauri && npx --yes @tauri-apps/cli@2.11.0 ios build --debug --target aarch64-sim --no-sign",
    "build:electron-lts": "node scripts/build.mjs",
    "pack:electron-lts": "pnpm --filter @dsh/desktop pack:current",
    "pack:electron-lts:full": "pnpm --filter @dsh/desktop pack:current:full",
    "pack:desktop": "pnpm --filter @dsh/desktop pack:current",
    "pack:desktop:full": "pnpm --filter @dsh/desktop pack:current:full",
    "pack:desktop:win": "pnpm --filter @dsh/desktop pack:win",
    "pack:desktop:win:full": "pnpm --filter @dsh/desktop pack:win:full",
    "pack:desktop:mac": "pnpm --filter @dsh/desktop pack:mac",
    "pack:desktop:mac:full": "pnpm --filter @dsh/desktop pack:mac:full",
    "pack:desktop:linux": "pnpm --filter @dsh/desktop pack:linux",
    "pack:desktop:linux:full": "pnpm --filter @dsh/desktop pack:linux:full",
    "pack:vscode": "pnpm --filter ./apps/vscode run pack:vsix",
    "parity": "pnpm --filter @dsh/parity-tests test",
    "dev:dsh": "node --import tsx scripts/dev-dsh.mjs",
    "start:electron-lts": "pnpm --filter @dsh/desktop start"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "electron"
    ]
  }
}
